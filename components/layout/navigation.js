import { Box, Container } from "@mui/material";
import React from "react";

const Navigation = () => {
  return <Container maxWidth="lg" sx={{ border: "2px solid red" }}></Container>;
};

export default Navigation;
